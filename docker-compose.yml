version: '3.9'

services:
  django_web:
    build: .
    container_name: web-server
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - vols:/home/django/data
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
volumes:
  vols: